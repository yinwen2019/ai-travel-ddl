/** Active view mode */
export type ViewMode = 'conference' | 'year'

/** DDL urgency level for color coding */
export type DDLUrgency = 'expired' | 'travel' | 'urgent' | 'near' | 'ample'

/** Progress bar node state */
export type ProgressNodeState = 'done' | 'current' | 'future'
